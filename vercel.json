{
  "devCommand": "uvicorn bot:app --host 0.0.0.0 --port 8000",
  "builds": [
    {
      "src": "bot.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "bot.py"
    }
  ]
}
